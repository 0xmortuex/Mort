// Iterative factorial with a typed accumulator.
fn factorial(n: int) -> int {
    let result: int = 1;
    let i = 1;
    while i <= n {
        result = result * i;
        i = i + 1;
    }
    return result;
}

fn main() -> int {
    print(factorial(5));   // 120
    return 0;
}
