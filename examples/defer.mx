fn choose(flag: bool) -> i64 {
    defer println("outer cleanup");
    if flag {
        defer println("inner cleanup");
        return 42;
    }
    return 0;
}

fn main() -> int {
    print(choose(true));
    return 0;
}
